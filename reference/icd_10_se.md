# Swedish version of the ICD-10 diagnoses code tree

A dataset including the following column:

- `pathString`:

  A string identifying all the parents of a node. Each parent is
  separated by a `/`.

## Usage

``` r
data(icd_10_se)
```
